# CampusLink 项目约束

## 项目定位与分工

- 这是小组课程设计，方案已确定，当前用户负责第一个接棒阶段的公共框架搭建。
- 以 README.md 和 docs/ 中的既定技术栈、框架设计及模块分工为基础；用户最新要求及本文件约束优先于先前计划中的冲突内容。
- 目标是交付同学能直接运行、理解并接着开发的框架，保持简单、实用、易交接。不擅自重新设计产品或扩大工程范围。
- 公共框架负责注册登录、公共用户资料、统一请求与响应、公共布局与样式、首页入口、个人中心及基础图片上传。
- 校园拼车、校园悬赏、二手书由后续同学自行设计和实现。这里只预留独立目录、路由入口和必要占位，不设计其业务页面、业务流程、数据模型或业务接口实现。
- 修改公共框架与业务模块的分工边界，必须有用户明确指令。

## 页面与代码表达

- 网页只呈现面向校园用户的产品内容，不出现开发版本标签、轮次、阶段进度、验收说明或开发计划。
- 源代码的注释、命名和提示文案不以开发版本或轮次组织，不写“某轮完成”“下一阶段实现”等过程记录。
- 包管理器和工具要求的正常版本字段可以保留；不得将这些版本信息展示成产品标签。
- 对尚未实现的业务模块，必要时只使用简洁的“功能开发中”占位，不带课程开发过程说明。
- 启动步骤、接口约定和协作说明放在必要的项目文档中，不混进产品页面。开发进度在对话中说明，不持续新增阶段报告文件。

## 实施与验证

- 按既定方案直接搭建所需能力，不增加无明确用途的抽象、依赖、工具、演示页或辅助工程。
- 每轮完成后直接验证该轮最终效果：启动项目、在浏览器走通关键操作、确认相关接口和数据能正常工作。
- 必要时运行一次前端构建检查；遇到实际失败再定位、修复和复测。
- 不为了凑覆盖率、穷举参数或预防假想问题新增几十项自动化测试；不专门扩建测试框架、引入测试依赖或创建复杂测试环境。
- 默认不新增自动化测试，除非用户明确要求，或确有无法用简短最终效果验证定位的具体问题；即使需要，也只做最小必要验证。
- 不反复运行已经通过且未受后续修改影响的检查，不输出冗长工具日志，节省时间和 token。
- 每轮交付用简短说明交代可用功能、最终效果验证结果和实际未完成项，等待用户检查后再进入下一轮。

## 范围控制与暂停

- 不实现聊天、支付、积分、评价、复杂权限、后台管理、地图、第三方登录或 AI 功能，不引入 Redis、Celery、消息队列、微服务、Docker 或 Kubernetes。
- 不擅自部署、发布、提交或推送代码，不覆盖用户已有修改。
- 用户要求暂停或只调整约束时，仅处理约束，不继续功能开发、测试或顺手清理。
- 现有实现如与本文件冲突，应在用户恢复对应实施工作后做针对性修正，不因记录约束而立即进行大范围改写或删除。

## 课程文档范围

- 需求说明书、数据库设计说明书、程序设计说明书、测试报告、系统部署维护说明、用户手册和工作分配属于全组项目材料，不能限定为公共框架个人交付。
- 当前负责人填写公共部分，并为 Ride、Quest、Book 和系统集成预留明确章节，由对应成员接续补齐；不预设其业务设计，不虚构完成情况。
- docs/framework.md 单独描述公共框架负责人的工作，须与全组文档定位区分。
